import { Typography } from '@mui/material';

export default function Dashboard() {
  return (
    <main>
      <Typography sx={{ my: 3 }} variant="h4">
        统计面板
      </Typography>
    </main>
  );
}
